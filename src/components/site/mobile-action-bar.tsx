import { Link } from "@tanstack/react-router";
import { CalendarDays, Info, Phone } from "lucide-react";
import { phoneHref, site } from "@/data/site";
import { useT } from "@/hooks/use-t";

export function MobileActionBar() { const t = useT(); return <nav className="fixed inset-x-0 bottom-0 z-50 grid h-16 grid-cols-3 border-t border-hero-foreground/15 bg-surface-deep text-hero-foreground md:hidden" aria-label={t.mobileBar.info}><Link to="/planning" className="motion-button grid min-h-11 place-items-center text-xs"><span className="flex flex-col items-center gap-1"><CalendarDays className="size-4 text-primary" />{t.mobileBar.planning}</span></Link><a href={phoneHref(site.phones[0])} className="motion-button grid min-h-11 place-items-center border-x border-hero-foreground/15 text-xs"><span className="flex flex-col items-center gap-1"><Phone className="size-4 text-primary" />{t.mobileBar.call}</span></a><Link to="/demande-dinformations" className="motion-button grid min-h-11 place-items-center text-xs"><span className="flex flex-col items-center gap-1"><Info className="size-4 text-primary" />{t.mobileBar.info}</span></Link></nav>; }